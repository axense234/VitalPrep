// React
import { FC } from "react";
// Types
import EntityType from "@/core/types/entity/EntityType";

const EntityInfoAppearances: FC<{
  entityId: string;
  entityType: EntityType;
}> = ({ entityId, entityType }) => {
  return <div>EntityInfoApperances</div>;
};

export default EntityInfoAppearances;
